import { StyleSheet } from "react-native";

const PictureMessageStyle = StyleSheet.create({
  messageImage: {
    width: 200, // You can adjust this based on your design
    height: 200, // Adjust accordingly
    borderRadius: 10,
    marginTop: 5,
    resizeMode: "cover",
  },
});

export default PictureMessageStyle;
